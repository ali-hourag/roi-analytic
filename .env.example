# The contact form submits via Netlify Forms — no environment variables needed.
# Configure the notification email in the Netlify dashboard:
# Site settings → Forms → Form notifications → Add notification → Email notification → admin@roianalytic.info
